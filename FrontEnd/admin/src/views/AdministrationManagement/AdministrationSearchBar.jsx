import SearchBar from "components/SearchBar";

function AdministrationSearchBar({ onChange }) {
  return <SearchBar onChange={onChange} sx={{ padding: 2 }} />;
}

export default AdministrationSearchBar;
